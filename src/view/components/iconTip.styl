.IconTip {
}

.IconTip--error {
  .IconTip-icon {
    color: #ec5b62;
  }
}

.IconTip-icon {
  color: #707070;
}

.IconTip-tooltip {
  // Important when using an IconTip inside a heading so that bold doesn't leak through to
  // the tooltip text.
  font-weight: normal !important;
}
